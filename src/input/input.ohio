<< Calculate area of a circle >>

beta pi = 3.14159;

func myFunc() { }

code {
    alpha radius = capture("Radius: ");
    beta area = pi * radius * radius;

    char error
    error = c

    flip (area > 50) {
        echo("Large circle");
    } flop {
        echo("Small circle");
    }
}
